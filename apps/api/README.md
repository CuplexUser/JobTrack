# @jobtrack/api

Internal API server for [JobTrack](https://github.com/CuplexUser/JobTrack) — a personal
job-application tracker with hybrid semantic search and duplicate detection. Consumed by the
`jobtrack` CLI, which runs it alongside the web UI as one background process.

Not meant for standalone use outside that package; no independent API stability guarantees.
