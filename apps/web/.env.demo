# Loaded by `vite build --mode demo` (the `build:demo` script). Switches `apps/web/src/api/index.ts`
# from the real, fetch-backed API to the in-browser demo — see `src/api/demo-client.ts`.
VITE_DEMO=true
