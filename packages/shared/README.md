# @jobtrack/shared

Internal shared types and Zod schemas for [JobTrack](https://github.com/CuplexUser/JobTrack)
— a personal job-application tracker. Consumed by `@jobtrack/api` and the `jobtrack` CLI.

Not meant for standalone use outside those packages; no independent API stability guarantees.
